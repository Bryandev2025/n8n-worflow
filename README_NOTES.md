Notes:
- Replace src/assets/hero.jpg with a real image file (same name) if desired.
- This template avoids adding react-router to keep dependencies minimal; swap in react-router if you need full routing.
- Consider adding automated tests and CI for production readiness.
